{
  "$schemaNote": "Module Contract (Gate 12) fields per entry: id, route, title, density, authRequirement, designPattern, entryPoint, businessSource, migrationStatus. Manifest fields (Gate 39) add: name, approvedReference, functionalSource, migrationWave, risk, notes. AUTH FOUNDATION Phase 2B adds: permissionId (the real modulos_portal.id this module maps to, or null) and authMode (one of LOGIN_REQUIRED, PERMISSION_MATRIX, MASTER_ONLY, ANALISTA_OR_MASTER, SEPARATE_AUTHORITY -- frontend-known constants, never dynamically read from a backend value; see assets/js/shell.js's route guard). Merged into one object per module below so a Wave doesn't have to invent structure twice.",
  "migrationStatusEnum": [
    "NOT_MIGRATED",
    "IN_PROGRESS",
    "PARITY_PENDING",
    "VISUAL_PARITY_PENDING",
    "UAT_PENDING",
    "HUMAN_APPROVED",
    "FROZEN"
  ],
  "statusNote": "VISUAL_PARITY_PENDING (Gate 30, PORTAL-NEXT-03): implementation exists, visual comparison against an Approved Reference not yet passed. Distinct from PARITY_PENDING, which is about functional/data parity. A module moves VISUAL_PARITY_PENDING -> UAT_PENDING only after its structural/visual comparison test suite actually passes — never skipped, never auto-promoted to HUMAN_APPROVED regardless of technical PASS (only a human can do that).",
  "note": "functionalSource points at the REAL V1 location identified during PORTAL-NEXT-01's audit. Nothing at those paths was read, imported, or ported this phase — see tests/business-logic-scanner.py. approvedReference ids are design-system-2.1 approvedReferences[] ids, resolved not copied — see tests/foundation-regression.py's reference-resolution check.",
  "modules": [
    {
      "id": "landing",
      "permissionId": null,
      "authMode": "LOGIN_REQUIRED",
      "name": "Landing",
      "route": "/landing",
      "title": "Portal F&I — Grupo Brabus",
      "density": "LOW",
      "authRequirement": "none (entry surface)",
      "designPattern": "module-landing (Human Approved Executable Reference)",
      "entryPoint": "assets/js/landing.js + assets/css/landing.css + config/landing-groups.json (PORTAL-NEXT-03)",
      "businessSource": "N/A (no business logic on Landing — verified 0 backend calls, Gate 41)",
      "functionalSource": null,
      "approvedReference": "module-landing",
      "referenceAuthorityStatus": "HUMAN_APPROVED (unchanged — design-system-2.1's own field, not altered by V2)",
      "responsiveRemediation": "PORTAL-NEXT-07.6 — mobile category nav (.fNav, was overflow-x:auto, real DOM overflow up to 339px) recomposed to flex-wrap; 0 business, 0 desktop drift (landing-composition-regression.py 20/20, 0.0pp at 1366/1920). RESPONSIVE: UAT_PENDING pending human sign-off — see docs/HUMAN-UAT-RESPONSIVE-REMEDIATION.md. BUSINESS status (HUMAN_APPROVED) unaffected.",
      "migrationWave": 1,
      "risk": "LOW",
      "migrationStatus": "HUMAN_APPROVED",
      "humanApprovalNote": "PORTAL-NEXT-04, Gate 1: human decision recorded (\"Landing V2: HUMAN APPROVED\") — metadata-only update, per this Wave's explicit instruction. Landing implementation files (landing.js/landing.css/landing-groups.json/index.html/brabus-logo.png) are byte-identical before and after this update — see REPORT.md's PORTAL-NEXT-04 entry for the hash re-verification. FROZEN as of this status: any future change requires a Design System Change Proposal + new human approval (Skill's Human Approved Freeze rule).",
      "notes": "PORTAL-NEXT-03: transplanted (Gate 8 method) from the Approved Reference — same class names, same DOM nesting, same interaction model, same Parametric Reactive config, same Context Beam behavior. Structural/visual comparison PASSED (20/20 checks, tests/landing-composition-regression.py)."
    },
    {
      "id": "shell-admin",
      "permissionId": "painelMaster",
      "authMode": "MASTER_ONLY",
      "name": "Portal Shell / MASTER Admin Panel",
      "route": "/admin",
      "title": "Painel Master",
      "density": "MEDIUM-HIGH",
      "authRequirement": "session + MASTER role (server-enforced)",
      "designPattern": "no Approved Reference yet — largest, most complex real surface (389KB portal-app.js)",
      "entryPoint": "assets/js/shell-admin.js + assets/css/shell-admin.css + assets/js/adapters/master-users-provider.js + master-users-view-model.js (Painel Master Phase 2A — Usuários subarea only)",
      "businessSource": "N/A (admin operations, not financial business logic)",
      "functionalSource": "assets/js/portal-app.js (ia-reconciliation-v2-local) — a real 17-tab admin workspace (Usuários, Acessos aos Módulos, Revisões Cadastrais, Pendências Cadastrais, Configurações, Períodos de Comissão, Férias/Ausências, Mudança de Loja, Gestão de Bases, Gestão dos Simuladores, Fechamento de Competência, Histórico de Competências, Relatórios RH/DP, Métrica Analista, Utilização dos Simuladores, Auditoria, Futuras Funcionalidades) — corrected count; this entry previously listed only 6 tabs (Painel Master Phase 0/1 audit finding, PORTAL-MASTER-01 Gate 21/22).",
      "approvedReference": null,
      "migrationWave": 1,
      "risk": "HIGH",
      "migrationStatus": "UAT_PENDING",
      "humanApprovalNote": "Painel Master Phase PM-6A.1 (2026-09-07): reconciled the Human UAT ledger for all 13 required top-level capabilities against both repo-persisted evidence and the historical conversation record — see docs/HUMAN-UAT-PAINEL-MASTER.md for the full 13-row matrix, subcapability breakdown, provenance and confidence ratings. Result: 11/13 capabilities HUMAN_APPROVED_LOCAL (Usuários [parent promoted, all 7 subcapabilities exercised], Acessos aos Módulos, Pendências Cadastrais, Auditoria, Gestão de Bases, Gestão dos Simuladores, Configurações, Períodos de Comissão, Férias/Ausências, Mudança de Loja [post-fix; historical HUMAN_REJECTED for horizontal overflow preserved, not discarded], Utilização dos Simuladores); 2/13 MIXED (Fechamento de Competência: UI/local flow HUMAN_APPROVED_LOCAL, real production close HUMAN_PENDING_FIRST_REAL_EVENT since 0 real closings exist; Histórico de Competências: Abrir Competência/PDF-Print/Reabrir HUMAN_APPROVED_LOCAL, Exportar RH/DP COMPLETE HUMAN_PENDING_FIRST_REAL_EVENT since 0 real COMPLETE closings exist, Exportar RH/DP LEGACY_PARTIAL HUMAN_EVIDENCE_NOT_RECONCILED). migrationStatus promoted from IN_PROGRESS to UAT_PENDING on this basis (technical: 13/13 TECH_READY per Phase PM-6A; human: substantially closed at 11/13 clean + 2/13 legitimately mixed, same UAT_PENDING precedent already used by Score for a comparably substantial-but-not-fully-closed state) — this is NOT a promotion to HUMAN_APPROVED for the module as a whole, which Phase PM-6A.1's own brief explicitly prohibits absent one single human decision covering the entire module; no such decision exists on record.",
      "migratedScope": "USERS_ACESSOS_AUDITORIA_AND_PENDENCIAS — Usuários (list/search/filter/detail, invite/edit/block, real mutations HUMAN_APPROVED as of Painel Master Phase 2D; detail moved into the shared contextual modal PM-4B.4, HUMAN_APPROVED_LOCAL) and Acessos aos Módulos (module x profile x department permission matrix, real mutations technically homologated Painel Master Phase 3B and HUMAN_APPROVED Painel Master Phase 3C — see docs/MASTER-USERS-RPC-CONTRACT-CAPTURE.md and Phase 3A/3B/3C reports for both RPC contracts). Auditoria (read-only admin-action-log, real data since Painel Master Phase PM-4B, detail modal since PM-4B.3, Human-approved locally) and Pendências Cadastrais (registration-governance work queue over the 7 already-deployed master_cadastro_*/master_cadastro_alerta_* RPCs, real data since Painel Master Phase PM-4C.2, no new backend — see PM-4C.1's own audit report) were migrated in later phases but this field was not updated at the time (Painel Master Phase PM-4C.2 registry-drift correction, Gate 67) — both are now listed here as real, migrated scope, not left implicit. Painel Master Phase PM-5C added Gestão de Bases (Bases 01/02/03 + Colaboradores operational-data ingestion), reusing 8 already-deployed, already-audited master_operational_* RPCs recovered live from production (no new backend, no new migration) — HUMAN_APPROVED_LOCAL as of commit d647f96 (Painel Master completion 5/13). Painel Master Phase PM-5D added Gestão dos Simuladores (10 real financial-config families across ZeroKM/Seminovos — Financiamento Linear, Taxas Subsidiadas, Taxa Botão, Simulador Balão ×2, Plano Coparticipado + its own Matriz de Coeficientes, Semestral Triton & Outlander, Financiamento Seminovos, Simulador de Antecipação shared across both groups), reusing 10 write RPCs (master_simulador_commit_*, one overloaded) + 1 read (master_simulador_listar_bases) recovered live from production (none of these 11 functions nor their 10 underlying tables exist anywhere in this repo's Git history — a bigger gap than Gestão de Bases' own — read via pg_get_functiondef, no new backend) — TECHNICALLY HOMOLOGATED as of this Phase (38/38 new contract-fidelity tests green, homologation-mode write-safety gate independently confirmed identical to Gestão de Bases' own mechanism, not assumed), but NOT YET Human-approved — this field intentionally does not conflate 'technically done' with 'Human-approved'; the Painel Master completion count stays at 5/13 until the Human explicitly homologates this capability. This admin migration deliberately does NOT connect the V2 simulators' (simulador-novos/simulador-seminovos, see their own registry entries) calculation engines to this data — those are already HUMAN_APPROVED and explicitly FROZEN (\"Fechamos os simuladores.\") with 0 backend connection by their own prior, separate, deliberate decision; admin-capability migration and simulator real-config integration remain two distinct, not-yet-merged Waves. Painel Master Phase PM-5E added Configurações (13 real global commission-tier parameters in public.configuracoes, e.g. share_minimo/spf_liquido_percentual/faixa-de-comissão-por-perfil — confirmed live that none of the 13 currently has a stored row at all, every one is still running on its code-level default) and Períodos de Comissão (the periodos_comissao business object — id/nome_periodo/data_inicio/data_fim/status/periodo_atual/ativo/criado_por — CREATE/SET_CURRENT/SET_ACTIVE/ARCHIVE only; SET_STATUS deliberately not exposed, no reachable V1 UI caller exists for it, the only path to FECHADO is the separate out-of-scope Fechamento de Competência flow, confirmed live to flip periodos_comissao.status as its own side effect). Both reuse existing, already-deployed RPCs (operational_portal_config/master_update_portal_config; master_admin_reference_data/master_admin_manage) recovered live via pg_get_functiondef (several bodies absent from Git) — no new backend. Confirmed INDEPENDENT of each other (no shared table, no shared RPC argument, PM-5E Gate 22) and implemented as two distinct sections accordingly, never merged into one screen. UNLIKE Gestão de Bases/Simuladores, NEITHER capability has any homologation-mode write-safety gate — confirmed live, not assumed — every save/action here is a REAL write against the real backend on any host, including localhost; automated tests mock the transport exclusively. TECHNICALLY HOMOLOGATED as of this Phase (26/26 + 34/34 new contract tests green, full existing regression and simulator-parity suites green), but NOT YET Human-approved — the Painel Master completion count stays at 5/13 until the Human explicitly homologates each capability (they may be approved independently, in either order). Of the other 15 real V1 admin tabs, 5 remain the actual V2 migration target (Férias/Ausências, Mudança de Loja, Fechamento de Competência, Histórico de Competências, Utilização dos Simuladores) and 4 are explicitly excluded from V2 by product decision — see 'productScopeExclusions' below, not silently dropped. Painel Master Phase PM-5F added Férias/Ausências (the ausencias_analistas business object — cpf/nome_analista_ausente, cpf/nome_analista_substituto, loja_origem/loja_coberta, data_inicio/data_fim, motivo free-text, ativo — CREATE/SET_ACTIVE/ARCHIVE only, no edit action exists in V1 either; a real, non-decorative financial-effect warning is surfaced client-side because the separate, out-of-scope, frozen operational_analyst_commission_metrics RPC reassigns real commission dollars from the absent analyst to the named substitute for any commission period overlapping the absence window) and Mudança de Loja - Vendedores (the mudancas_loja_vendedores business object — seller identity fields, loja_origem/loja_destino, data_inicio_origem/data_fim_origem/data_inicio_destino [no data_fim_destino column exists at all — the destination assignment is intentionally open-ended], departamento_origem/departamento_destino [hard-validated server-side against NOVOS/SEMINOVOS only], usuario_id [a real FK resolved server-side by CPF match] — CREATE/SET_DEPARTMENTS/SET_ACTIVE/ARCHIVE only, no delete action for either capability, no DELETE RLS policy on either table). Both reuse the exact same already-deployed dispatcher pair already used by Períodos (master_admin_reference_data/master_admin_manage, confirmed byte-identical live body across phases) — no new backend, no new migration. The strict forward-only CHAIN model enforced server-side for Mudança de Loja (destination_start must be exactly one day after origin_end; a seller's new record must originate from their own most-recent record's destination store/date) is mirrored client-side as non-blocking UX guidance only — the real RPC remains the sole authority. SET_DEPARTMENTS is implemented via this shell's own shared confirmation-modal component, deliberately NOT V1's native window.prompt() (editarDepartamentosMudancaLojaVendedor). UNLIKE Gestão de Bases/Simuladores, NEITHER new capability has any homologation-mode write-safety gate — confirmed live, not assumed — every action here is a REAL write against the real backend on any host, including localhost; automated tests mock the transport exclusively (78/78 new contract-fidelity tests green across both capabilities, full existing regression and simulator-parity suites green, full 11-width responsive matrix and long-content stress test run for both, not a representative subset). TECHNICALLY HOMOLOGATED as of this Phase, but NOT YET Human-approved — the Painel Master Human-approved completion count stays at 5/13 until the Human explicitly homologates each capability (they may be approved independently, in any order); the technically-homologated count reaches 10/13 with this Phase. Of the remaining 3 real V1 admin tabs not yet migrated after that Phase (Fechamento de Competência, Histórico de Competências, Utilização dos Simuladores), Painel Master Phase PM-5G audited Fechamento de Competência (contract fully reconciled: master_close_commission_period/master_reopen_commission_period/master_commission_closings/master_commission_snapshot/master_commission_snapshot_export, all confirmed live) but did NOT implement it — the real closing action requires reproducing V1's live commission-calculation engine client-side to build a valid payload, which would mean duplicating frozen financial logic; STOP audit-only per that Phase's own gate discipline, zero files changed, technically-homologated count unchanged at 10/13. Painel Master Phase PM-5H added Utilização dos Simuladores (READ-ONLY telemetry/adoption reporting — sessions, simulations, active time, never simulation content — over public.portal_module_sessions via master_simulator_usage_data(), confirmed live MASTER-only, RLS enabled with ZERO policies on the underlying table — the strongest access posture in this codebase, RPC-only for every role including MASTER). Confirmed real, complete write-side contract via 4 live-fetched RPCs (portal_telemetry_start_session/_heartbeat/_simulation/_end_session, 100% git-absent) called exclusively by the simulator surfaces themselves (external GitHub Pages deployments, not present in any local repo) — never by this admin screen, which exposes no mutation of any kind, matching V1's own real contract exactly; V2's own simulators confirmed to emit zero telemetry (no new instrumentation added, by design, per this Phase's own explicit scope limit). Real data-quality facts documented, not fixed: ~45% of real sessions never receive a close signal (no background sweep exists, only a lazy heartbeat-triggered timeout) and ~50% of sessions produce zero simulations (opening a simulator is not the same as running one). TECHNICALLY HOMOLOGATED as of this Phase (52/52 new contract-fidelity tests green, full mandatory 11-width responsive matrix, full existing regression and Cash Conversion/simulator-parity suites green), but NOT YET Human-approved — the Painel Master Human-approved completion count stays at 5/13 until the Human explicitly homologates each capability (they may be approved independently, in any order); the technically-homologated count reaches 11/13 with this Phase. Of the remaining 2 real V1 admin tabs not yet migrated (Fechamento de Competência, Histórico de Competências), neither was started this Phase beyond PM-5G's own audit-only work. migrationStatus alone (IN_PROGRESS, same enum value/precedent as Coparticipado/Gestão/Dashbi's own early real-data-integration phases) cannot carry this partial-scope nuance — this field exists specifically so it isn't lost.",
      "productScopeExclusions": "Painel Master Phase 4A inventoried all 17 real V1 admin tabs (MASTER_V1_CANONICAL_SURFACE_COUNT=17, unchanged historical fact). Ahead of Phase PM-4B, the product owner explicitly excluded 4 of the 15 not-yet-migrated tabs from V2 migration entirely (DO_NOT_MIGRATE_BY_PRODUCT_DECISION — a distinct category from MIGRATED/HUMAN_APPROVED/FAILED/PENDING): Revisões Cadastrais (SUPERSEDED_BY_PENDENCIAS_CADASTRAIS — lost its purpose once Pendências Cadastrais shipped), Relatórios RH/DP (REDUNDANT_WITH_EXISTING_MODULES), Métrica Analista / 'Auditoria da Métrica do Analista' (NO_CURRENT_PRODUCT_UTILITY), Futuras Funcionalidades (OBSOLETE_PRODUCT_SURFACE — static, stale roadmap, no backend). No V1 code, RPC, or backend was altered/removed by this decision — these surfaces remain fully intact and inspectable in ia-reconciliation-v2-local, only removed from the V2 migration backlog. MASTER_V2_TARGET_SURFACE_COUNT=13 (17 minus these 4, minus nothing else) going forward.",
      "landingTitle": "Painel Master",
      "landingDesc": "Administração de usuários e acessos do Portal F&I.",
      "notes": "V1's shell wraps this via successive stacked JS patches (see PORTAL-NEXT-01/ARCHITECTURE-AUDIT.md Gate 4) — V2's shell must NOT reproduce that accretion pattern. Includes the 'Auditoria' admin-action-log tab (PORTAL-NEXT-01.1 finding: real, but a tab, not a standalone module) — built and Human-approved locally as of Painel Master Phase PM-4B/PM-4B.3 (this 'still unbuilt' statement was stale, corrected Painel Master Phase PM-4C.2, Gate 67). Painel Master implements four of 17 real admin tabs so far (Usuários, Acessos aos Módulos, Auditoria, Pendências Cadastrais), all real-data, all real-mutation-homologated (Auditoria is read-only by design), all Human-approved locally — see 'migratedScope' above. The router is flat (one hash segment per module id, '#/shell-admin'; this entry's own 'route':'/admin' field is documentation-only, never consumed by code); future subareas are internal view state within shell-admin.js, not separate registry entries. Painel Master Phase PM-5B: 'landingTitle' corrected from the stale 'Auditoria / Painel Master' (a historical leftover from when Auditoria was the module's most recently-added sub-feature) to the neutral 'Painel Master', matching this entry's own 'title' field — it is read verbatim by the sidebar nav label, the Landing module card, and the breadcrumb (landing.js), none of which are section-aware; a single static string cannot correctly name one of 4 internal sections, so it must name the module itself. No route/permission/id field touched."
    },
    {
      "id": "score",
      "permissionId": "analiseScoreVendedores",
      "authMode": "PERMISSION_MATRIX",
      "name": "Score (Análise de Score Vendedores)",
      "route": "/score",
      "title": "Análise de Score Vendedores",
      "density": "HIGH (ranking table) / MEDIUM (summary+detail) — see docs/SCORE-VISUAL-AUTHORITY.md",
      "authRequirement": "session + role scope, server-enforced (Score Real Data Integration Phase 2A: real transport wired against operational_score_coparticipated_data, shared with Coparticipado, gated server-side by analiseScoreVendedores; Phase 2B added the real period-filter contract; fixture transport remains the deterministic-test default per isRealTransport())",
      "designPattern": "score (Approved normative pattern — NO executable Approved Reference exists; NORMATIVE COMPLIANCE reported, not VISUAL PARITY, per Gate 43)",
      "entryPoint": "assets/js/score.js + assets/css/score.css + assets/js/adapters/score.adapter.js + assets/js/adapters/score-real-provider.js + assets/js/adapters/score-real-view-model.js (PORTAL-NEXT-04; real-data files added Score Real Data Integration Phase 2A/2B)",
      "businessSource": "modules/score.html — calcScores() — EXTRACTED this phase, byte-identical, from origin/main (NOT the local clone, proved divergent — see docs/SCORE-ENGINE-AUDIT.md)",
      "functionalSource": "origin/main:modules/score.html:calcScores (portal-financiamento-brabus-secure) == live production (PROVED, PORTAL-NEXT-03.1 + re-verified this Wave)",
      "approvedReference": "score",
      "responsiveRemediation": "PORTAL-NEXT-07.6 — this Wave's own brief assumed Score was already HUMAN_APPROVED/FROZEN; migrationStatus here shows UAT_PENDING (unchanged since PORTAL-NEXT-04, its own Design System band conflict never resolved) — flagged, not silently corrected. Table (.scTableWrap, real DOM overflow up to 174px) got table-layout:fixed + wrap + a <=480px vertical stack; .scNameText's ellipsis-truncation (real info loss, 2-4px residual overflow past it) replaced with wrapping. 0 business change (score-parity-test.py 12/12); score calculation/bar/band-conflict untouched (Gate 16, not resolved this Wave either). RESPONSIVE: UAT_PENDING — see docs/HUMAN-UAT-RESPONSIVE-REMEDIATION.md. PORTAL-NEXT-07.6.2 — human visual UAT rejected that <=480px stacking: a generic flex-wrap packed however many data-th cells fit per row, still compressing real content (Vendedor names wrapping narrow, Loja/Depto/Score/Financ. cramped). Replaced with an explicit one-field-per-row record (no flex-wrap packing decision left to content length); Vendedor gets full-width bold emphasis, Score gets its own bordered/emphasized block with the approved bar intact, no new bands/colors/thresholds introduced. 0 business change (score-parity-test.py 12/12 unchanged; score.adapter.js untouched); only score.css changed. RESPONSIVE remains UAT_PENDING (a defect was found and fixed, not yet re-approved). PORTAL-NEXT-07.6.4 — human UAT rejected 07.6.2 a SECOND time (after a PORTAL-NEXT-07.6.3 investigation had ruled out server/cache staleness and re-verified 07.6.2's code as correct by every computed-style/DOM check — rejected again anyway). Strategy changed: CSS-transforming the desktop <table> was abandoned; a dedicated non-table mobile card renderer (renderMobileCards()/.scMobileOnly, plain <div> markup, 0 table/tr/td anywhere) now renders alongside the untouched desktop table (renderDesktopTable()/.scDesktopOnly), both fed by the same computed `rows` array, exactly one visible at a time (proven via computed style: .scTable's own display computes 'none' at 390px — the table structurally stops existing at that breakpoint, not just visually altered). A real overflow bug (52px, large_values fixture, .scMeterTrack's width:100% not releasing the base rule's flex-shrink:0) was caught by an exhaustive 12-fixture x 7-viewport sweep and fixed. 0 business change (score-parity-test.py 12/12; score.adapter.js untouched). RESPONSIVE remains UAT_PENDING pending a fresh human visual pass on this structurally different presentation. PORTAL-NEXT-07.7B — RESPONSIVE promoted to HUMAN_APPROVED/FROZEN this Wave (per this brief's own explicit Gate 34 instruction, not a claim that a separate earlier approval message existed). Implements the human-selected Score Band (docs/SCORE-BAND-DISCOVERY-07-7A.md Option 1, docs/SCORE-BAND-NORMATIVE-07-7B.md): one authoritative classifyScoreBand() in score.js, consumed identically by both renderers, 0 duplicated threshold logic. Absolute 0-299/300-549/550-749/750-899/900-1000 boundaries on the unchanged 0-1000 engine scale (score.adapter.js hash unchanged, 0 diff). Invalid/non-finite Score (the NaN defect from 07.7A) returns no band, verified against the real fixture that produces it. Colors reuse existing semantic tokens (critical/warning/success), PERFORMANCE neutral, ELITE distinguished from ALTA PERFORMANCE by weight/border not a new hue. tests/score-band-test.py (new): 24/24 (boundaries, invalid inputs, representative real scores, real NaN fixture). No-horizontal-scroll re-verified at all 7 viewports; long labels confirmed word-wrapping only. Desktop fidelity preserved (additive inside the existing Score cell, 0 new column). Score Band Business Rule: HUMAN_APPROVED. Score Band Visual: UAT_PENDING pending human look. PORTAL-NEXT-07.7C — the human confirmed PORTAL-NEXT-07.7B's visual UAT (brief's own explicit statement, same approval-channel pattern as PORTAL-NEXT-07.6.1's \"Aprovado.\"): Score Band Business Rule, Score Band Visual, and Score Responsive all promoted to HUMAN_APPROVED/FROZEN. This Wave then fixed the pre-existing receitaSPF-driven NaN Score defect (PORTAL-NEXT-07.7A Gate 19, left untouched by design in 07.7B). Root cause: calcScores()'s o.retorno += f.retorno + f.receitaSPF has no fallback for a missing receitaSPF. Traced conclusively to production's own processFins() (read from a locally-saved origin/main copy, 0 live production connection) building receitaSPF via a numeric-seeded .reduce(...,0) — real production data can never produce a fins[] record with receitaSPF missing, so \"missing\" is conclusively \"zero SPF revenue\", not \"unknown\". Fix: a normalization boundary in score.adapter.js using production's own byte-identical asNumber() parser, applied to fins[].receitaSPF BEFORE the still byte-for-byte-untouched calcScores() is called (verified via git diff — 0 lines removed/changed inside the extracted calcScores() body). score-parity-test.py: 11/12 exact match to the untouched production reference (the 1 intentional divergence, missing_optional_data, IS the fix — that fixture now returns a real finite score where the unmodified reference still returns NaN, by design). score-band-test.py: 24/24 (one assertion updated to the corrected, finite result; the classifier's own invalid-input contract is unchanged). New score-spf-defect-test.py: 16/16 (live before/after proof + 13-case missing-value matrix + Gate-13 zero-preservation check). 0 change to Score formula weights/thresholds. Other modules: 0 diff. See docs/SCORE-RECEITA-SPF-NONFINITE-07-7C.md.",
      "migrationWave": 2,
      "risk": "MEDIUM",
      "migrationStatus": "HUMAN_APPROVED",
      "landingTitle": "Análise de Score Vendedores",
      "landingDesc": "Ranking de performance F&I por vendedor, com score, detalhes e auditoria.",
      "functionalParity": "PASS — 12/12 golden fixtures, exact match (tests/score-parity-test.py)",
      "normativeCompliance": "FAIL on exactly one point (band ALTO/BOM/BAIXO does not exist in production, not invented — formalized as docs/DS-CHANGE-PROPOSAL-SCORE-BAND-01.md, STATUS: HUMAN REVIEW REQUIRED). PASS on every other MUST, including the linear score meter (PORTAL-NEXT-04.1 — transplanted from module-landing-approved's Human Approved .rankScoreBar component, single non-band-derived fill color), confidence, breakdown, explanation, no-color-only-status, no gauge/speedometer.",
      "visualParity": "NOT APPLICABLE — no executable Approved Reference exists for Score (Gate 43)",
      "notMigratedThisWave": "isScoreSellerEligible() (needs backend vendor registry) and openSellerDetails() (per-transaction drill-down modal, overlaps Balão/Parcela simulator-domain rendering, explicitly out of scope — Gate 34). See docs/SCORE-EXTRACTION-TRACE.md.",
      "notes": "calcScores() changed substantially between what PORTAL-NEXT-01 originally audited (local clone) and real production (new confidence-dampening mechanism, redistributed weights, changed plan-mix methodology) — re-audited from the correct source this Wave, see docs/SCORE-ENGINE-AUDIT.md.",
      "humanApprovalNote": "Score Band Business Rule, Score Band Visual, and Score Responsive: all HUMAN_APPROVED/FROZEN as of PORTAL-NEXT-07.7C (Band Visual and Responsive reconfirmed here; Band Business Rule granted PORTAL-NEXT-07.7B). PAINEL MASTER V2 PHASE 3B, GATE 2 (registry drift correction): this migrationStatus field was left at 'UAT_PENDING' after PORTAL-NEXT-07.7C even though a separate, later effort — Score Real Data Integration Phase 2A (real transport against operational_score_coparticipated_data)/2B (real period-filter contract)/2C (human UAT closure) — reached real-data human approval for Score's overall Business/Functional status. That closure was never reflected here, a real staleness confirmed by direct comparison against score-real-provider.js/score-real-view-model.js (present, dated 2026-09-04) and this field's own prior text. Corrected to HUMAN_APPROVED on that basis; no new human decision was fabricated by this edit — Score Real Data Integration Phase 2C's own human UAT is the approval being recorded, only late. SCORE_LOCAL_CALENDAR_DATE_PRESET_FIX (2026-09-07, commit b6affbb, see docs/CHANGE-PROPOSAL-SCORE-LOCAL-CALENDAR-DATE-PRESET.md): a separate, later, real-data-era audit found computePreset()'s quick-period buttons (Mês atual/Mês anterior/Últimos 6 meses/Último ano) serialized correct local-time Date objects via .toISOString() (a UTC round-trip), shifting the reported calendar date near timezone boundaries -- the same defect class already fixed for Gestão/Dashbi during FI-UX-1, left as known-but-unfixed debt for Score at the time. migrationStatus was moved to UAT_PENDING to reopen the freeze narrowly for this one correction (0 formula/drill-down/backend/authorization change). Human tested the fixed V2 localhost build and responded, verbatim, \"Score validado\" -- classification SCORE_LOCAL_CALENDAR_FIX_HUMAN_APPROVED_LOCAL. migrationStatus restored to HUMAN_APPROVED on this basis; SCORE_DRILLDOWN_ALREADY_CLOSED (a separate, unrelated, already-closed question) was neither reopened nor reinterpreted by this approval."
    },
    {
      "id": "coparticipado",
      "permissionId": "coparticipadoPortal",
      "authMode": "PERMISSION_MATRIX",
      "name": "Coparticipado",
      "route": "/coparticipado",
      "title": "Gestão de Coparticipados & Subsidiados",
      "density": "MEDIUM (filtered table + 3-stat summary on the Subsidiados view) — see docs/COPARTICIPADO-ENGINE-AUDIT.md; data-table.md's HIGH-density Ambient-Motion-OFF rule applied to the table region regardless (0 canvas mounted on this route)",
      "authRequirement": "session + role scope, server-enforced (Coparticipado Phase 2, Real Data Integration Foundation: real transport wired against operational_score_coparticipated_data for sales/finance, shared with Score, gated server-side by analiseScoreVendedores -- as of Phase 1C the coparticipadoPortal matrix is fully aligned with it across every catalog profile; fixture transport remains the deterministic-test default per isRealTransport(). V2_COPART_GOVERNED_RATE_AUTHORITY_CORRECTION: the financial rebate RATE authority is now separately sourced from simulador_get_coparticipado -- the SAME governed, versioned ACTIVE managed batch Simulador de Novos' reference implementation consumes -- via coparticipado-governed-rates-provider.js, gated by the identical session/role scope, no new permission introduced. operational_score_coparticipated_data's own 'rates' field remains in its payload for backend-contract compatibility but is no longer read for calculation. See docs/CHANGE-PROPOSAL-V2-COPART-GOVERNED-RATE-AUTHORITY.md.)",
      "designPattern": "data-table (Approved normative pattern, same one Score uses) — NO executable Approved Reference exists for Coparticipado; NORMATIVE COMPLIANCE reported, not VISUAL PARITY, per Gate 43",
      "entryPoint": "assets/js/coparticipado.js + assets/css/coparticipado.css + assets/js/adapters/coparticipado.adapter.js (PORTAL-NEXT-05)",
      "businessSource": "modules/coparticipado.html — classifyPlan/processFins/processSales/matchB3/calcCoparticipacaoDetalhe and 5 more pure functions (40 total incl. display/filter helpers) — EXTRACTED this phase, byte-identical, from origin/main (NOT the local clone, proved divergent — see docs/COPARTICIPADO-ENGINE-AUDIT.md)",
      "functionalSource": "origin/main:modules/coparticipado.html (portal-financiamento-brabus-secure) == live production (PROVED, same byte-hash method as PORTAL-NEXT-03.1/04)",
      "approvedReference": null,
      "migrationWave": 3,
      "risk": "MEDIUM",
      "migrationStatus": "HUMAN_APPROVED",
      "humanApprovalNote": "PORTAL-NEXT-06, Gate 1: human decision recorded (\"Coparticipado V2: HUMAN_APPROVED / FROZEN\") — metadata-only update, per this Wave's explicit CONTEXT/HUMAN DECISION section. Implementation files (coparticipado.js/coparticipado.css/coparticipado.adapter.js) are byte-identical before and after this update — see REPORT.md's PORTAL-NEXT-06 entry for the hash re-verification. That original HUMAN_APPROVED/FROZEN status is preserved here as historical provenance, chronology step 1. V2_COPART_GOVERNED_RATE_AUTHORITY_CORRECTION (2026-09-07): explicit, narrowly-scoped human authorization to reopen this frozen module (chronology step 2 — reopened) and correct a proven financial rate-authority defect (the real transport's Coparticipação/Rebate calculation was sourced from operational_score_coparticipated_data's own 'rates' field, backed by table coparticipado_modelos_fi, proven stale since 2026-06-30 against the governed authority — see docs/CHANGE-PROPOSAL-V2-COPART-GOVERNED-RATE-AUTHORITY.md for the full proposal, evidence, tests, and rollback strategy). Not a visual redesign, not a formula change, not a Design System change — financial data-authority correction only; UI/formula/authorization/Subsidiado semantics all unchanged. Corrected authority: simulador_get_coparticipado -> ACTIVE governed managed batch (same authority Simulador de Novos' reference implementation is designed around), implemented in commit 9af543a (\"fix(v2): use governed copart rate authority\") — chronology step 3, technical implementation, classification V2_COPART_GOVERNED_RATE_AUTHORITY_TECHNICALLY_READY. migrationStatus was demoted to UAT_PENDING as an interim state pending new Human UAT of this specific correction — chronology step 4. POST-FIX HUMAN UAT (2026-09-07): Human explicitly tested the V2 localhost build after commit 9af543a and stated \"ok, validado.\" — chronology step 5, classification V2_COPART_GOVERNED_RATE_AUTHORITY_HUMAN_APPROVED_LOCAL. migrationStatus restored to HUMAN_APPROVED on this basis — chronology step 6, module REFROZEN. FROZEN as of this status: any future functional change again requires a new Design System Change Proposal + new human approval (Skill's Human Approved Freeze rule) — this refreeze does not carry forward blanket authorization for unrelated future changes. Known remaining debt, explicitly NOT resolved by this correction: COPART_TEMPORAL_RATE_RESOLUTION_DEBT (no rate-by-operation-date resolution, current ACTIVE rate is always used regardless of operation date) and V2_SIMULADOR_NOVOS_GOVERNED_RATE_PARITY_AUDIT_REQUIRED (Simulador de Novos still uses a hardcoded MODELS matrix, not this same governed RPC — a separate, independent future wave, not started here).",
      "responsiveRemediation": "PORTAL-NEXT-07.6 — the widest table in V2 (Coparticipados 13 cols / Subsidiados 11 cols), the most severe pre-existing case (still 469px of real DOM overflow at 1920px). table-layout:fixed + wrap + a <colgroup> floor-width/nowrap protection for atomic currency/percentage/date/chassi columns (a real mid-token wrap regression, e.g. 'R$ 155.00'+'0', was caught by re-screenshotting and fixed before being reported clean); <=900px recomposes into a full vertical record (every field shown — no authorized primary/secondary split exists for this record, none invented). 0 business change (coparticipado-parity-test.py 22/22); coparticipado.adapter.js byte-identical. This is a presentation-only change explicitly authorized by this Wave's own brief (the 'new human approval' required by the FROZEN note above, scoped exactly to responsive/no-scroll recomposition). RESPONSIVE: UAT_PENDING — see docs/HUMAN-UAT-RESPONSIVE-REMEDIATION.md. BUSINESS status (HUMAN_APPROVED) unaffected.",
      "landingTitle": "Gestão de Coparticipados",
      "landingDesc": "Classificação de planos Coparticipados e Subsidiados por vendedor, loja e departamento.",
      "functionalParity": "PASS — 22/22 golden fixtures, exact match (tests/coparticipado-parity-test.py), goldens generated by executing an independently re-extracted reference copy (tests/fixtures/_coparticipado-reference.js, built by a separate script/algorithm than the adapter assembly) in a real browser — not hand-calculated.",
      "normativeCompliance": "PASS on every data-table.md MUST checked this Wave: table stays table (no cardification), dense/hairline-separated, right-aligned tabular-nums numeric columns, no cell-background fills, no pill-per-cell, sticky first column + horizontal scroll on mobile, HIGH-density Ambient Motion OFF, keyboard-focusable interactive rows N/A (this table has no per-row interaction — see drill-down note below). Sortable-column visual indicator correctly left UNRESOLVED (not invented), matching that production's own Coparticipados/Subsidiados tables are not interactively sortable either.",
      "visualParity": "NOT APPLICABLE — no executable Approved Reference exists for Coparticipado (Gate 43)",
      "notMigratedThisWave": "Diagnóstico tab (renderVendorAlerts/collectUnknownVendors — vendor-registry data-quality feature, needs the real vendor registry backend, orthogonal to classification/crossing/exclusion/priority/filter/sort); export to Excel (exportarCoparticipados/exportarSubsidiados — needs xlsx-js-style + real file I/O, same 0-backend boundary as the Base01/02/03 Excel parsing deferral); the embedded stale openSellerDetails/openScoreDetails/calcScores block — CONFIRMED DEAD CODE in production (no tab/section wires #novos/#score into the real UI; renderNovos() is a literal no-op), same finding pattern as Score's own stale code, not migrated. See docs/COPARTICIPADO-EXTRACTION-TRACE.md.",
      "drillDown": "NOT APPLICABLE — verified by direct source read that production's own Coparticipados/Subsidiados tables have no per-row detail modal; the row already is the finest real granularity (one financing operation each). No drill-down was invented to fill this gap.",
      "notes": "Two real production views migrated verbatim (Fase UX-Grupo-3.0 Item 3's 'Visão Coparticipados' / 'Visão Subsidiados' switcher, same DATA.fins source, same r.plano field, no new classification) plus the real Loja/Departamento/date-range filters (same predicate as currentFiltered()). Landing placement: moved from 'deliberately not represented' (PORTAL-NEXT-03 Gate 39, while NOT_MIGRATED) into the existing 'Gestão' category this Wave (Gate 28, no grammar change) — see config/landing-groups.json."
    },
    {
      "id": "gestao",
      "permissionId": "gestao",
      "authMode": "PERMISSION_MATRIX",
      "name": "Gestão (analise-fi-grupo)",
      "route": "/gestao",
      "title": "Análise F&I do Grupo",
      "density": "MEDIUM-HIGH (KPI cards + dense tables, no charts)",
      "authRequirement": "session + role scope (NOT implemented this Wave — 0 backend, local fixtures only, same boundary as Score/Coparticipado)",
      "designPattern": "data-table (Approved normative pattern, same one Score/Coparticipado use) — NO executable Approved Reference exists for Gestão; NORMATIVE COMPLIANCE reported, not VISUAL PARITY (Gate 32/43)",
      "entryPoint": "assets/js/gestao.js + assets/css/gestao.css + assets/js/adapters/gestao.adapter.js + assets/js/adapters/gestao-fixture-provider.js + assets/js/lookups/store-display.js (PORTAL-NEXT-06; adapter/fixture-provider/lookups restructured, RPC-shaped local contract reconciliation)",
      "businessSource": "modules/analise-fi-grupo.html — normalizeRows/planTypeFromFields/buildStore/buildCpfAnalysis/buildPlanAnalysis/buildSpfExtraAnalysis and 33 more pure functions (38 total) — EXTRACTED this phase, byte-identical, from origin/main (this file, unlike Score/Coparticipado, is NOT divergent from the local clone — confirmed local==origin/main==live production — still read from origin/main per standing discipline; see docs/GESTAO-ENGINE-AUDIT.md/GESTAO-FUNCTION-MAP.md)",
      "functionalSource": "origin/main:modules/analise-fi-grupo.html (portal-financiamento-brabus-secure) == live production (PROVED, same byte-hash method as prior Waves)",
      "approvedReference": null,
      "migrationWave": 4,
      "risk": "MEDIUM",
      "migrationStatus": "HUMAN_APPROVED",
      "humanApprovalNote": "PORTAL-NEXT-07, Gate 1: human decision recorded (\"Análise F&I do Grupo: HUMAN_APPROVED / FROZEN\") — metadata-only update, per this Wave's explicit CONTEXT section. Implementation files (gestao.js/gestao.css/gestao.adapter.js) are byte-identical before and after this update. FROZEN as of this status: any future change requires a Design System Change Proposal + new human approval (Skill's Human Approved Freeze rule). SPF EXTRA 70% authority (docs/GESTAO-COMMISSION-DECISION.md) and the table-alignment fix (PORTAL-NEXT-06.1) are both part of this frozen baseline.",
      "responsiveRemediation": "PORTAL-NEXT-07.6 — 8 tables (up to 10 columns, real DOM overflow up to 819px at 320px) got table-layout:fixed + wrap + a <=768px vertical stack (shared row()/headerRow() helpers extended with an optional data-th parameter, non-breaking for any caller that omits it). 0 business change (gestao-parity-test.py 30/30, including all 4 SPF-rounding fixtures — Comissão Líquida SPF EXTRA = 70% untouched); gestao.adapter.js byte-identical. Presentation-only change explicitly authorized by this Wave's own brief. RESPONSIVE: UAT_PENDING — see docs/HUMAN-UAT-RESPONSIVE-REMEDIATION.md. BUSINESS status (HUMAN_APPROVED) unaffected.",
      "landingTitle": "Análise F&I do Grupo",
      "landingDesc": "Análise operacional F&I/FANDI consolidada do Grupo Brabus Mitsubishi.",
      "functionalParity": "PASS for all 38 extracted functions and every KPI/table, including 'Comissão Líquida SPF EXTRA' — 30/30 golden fixtures, exact match (tests/gestao-parity-test.py, includes 4 new SPF-rounding-audit fixtures added PORTAL-NEXT-06.1), goldens generated by executing an independently re-extracted reference copy (tests/fixtures/_gestao-reference.js) in a real browser.",
      "commissionImpact": "RESOLVED (PORTAL-NEXT-06.1, human UAT feedback on PORTAL-NEXT-06) — see docs/GESTAO-COMMISSION-DECISION.md, STATUS: HUMAN DECIDED. Authority: 'Comissão Líquida SPF EXTRA = Total SPF EXTRA × 70%', fixed and non-configurable for this metric specifically — the pre-existing byte-identical extraction (Gate 3 of that Wave: no second formula introduced). Historical investigation preserved in docs/COMMISSION-RULE-MAP.md: a second, genuinely configurable mechanism (spf_liquido_percentual) still exists in the separate Salários/Comissões commissionCalc() engine, confirmed unaffected by and not generalized from this decision.",
      "normativeCompliance": "PASS on every data-table.md MUST checked this Wave (same as Score/Coparticipado): tables stay tables, dense/hairline-separated, right-aligned tabular-nums, sticky first column + horizontal scroll on mobile, HIGH-density Ambient Motion OFF. KPI hierarchy reflects materiality (Gate 34): Produção cards are primary, executive KPIs secondary, support KPIs visually quietest — no giant uniform KPI wall. PORTAL-NEXT-06.1: numeric table headers and values now share one right-aligned axis in every Gestão table (0px measured diff across all 8 tables) — a real alignment bug found in human UAT and fixed via a shared header/value alignment contract, not per-cell offsets.",
      "visualParity": "NOT APPLICABLE — no executable Approved Reference exists for Gestão (Gate 32/43)",
      "notMigratedThisWave": "Real Supabase call to operational_fandi_dashboard — 0 backend/0 network this Wave either, by explicit Wave 2 brief decision (contract reconciliation and real backend integration are deliberately separate Waves). What changed: the LOCAL client-side classification/aggregation engine (raw Base01/02/03-row normalization, store canonicalization, plan classification, CPF-dedup proposal outcomes) is retired (assets/js/adapters/_gestao-legacy-upload-engine.js, DEAD LEGACY, not loaded) in favor of a local fixture PROVIDER that already speaks the RPC's own response shape (assets/js/adapters/gestao-fixture-provider.js) — so the only thing a future real-backend Wave needs to replace is that provider's loadGestaoFixture() call, not the adapter or the renderer. Model Analysis, Seller Analysis, FECHAMENTO, charts, export — still CONFIRMED ABSENT from this specific production module (unchanged from prior finding), see docs/GESTAO-KPI-CONTRACT.md.",
      "wave2Reconciliation": "PORTAL V2 SECURE-DELTA WAVE 2 (RPC-shaped local contract reconciliation) — authorized under this Wave's own brief as the scoped human-approval instrument for touching this frozen module (same governance pattern as PORTAL-NEXT-06.1/07.6's prior scoped changes). Backend contract (operational_fandi_dashboard) proven read-only against the deployed function in Wave 1, Secure commit 4d8ce1d410f5aa05d563a81876c7d5b45e455306 (HUMAN VALIDATED IN PRODUCTION, includes the ANALISTA-group-wide-store-scope business rule). Store IDENTITY is now exclusively the backend's canonical short codes (ABC/ALPHAVILLE/ANALIA FRANCO/BANDEIRANTES/BARRA FUNDA/EUROPA/GASTAO/NACOES) — assets/js/lookups/store-display.js provides presentation-only display names, never used for comparison/filtering/keying; the old lojaCurta() identity function (which produced display strings used AS identity, opposite direction from production) is retired with the rest of the legacy engine. gestao.adapter.js is now a thin response-shaping layer (numeric/array defaulting only) — 0 RBAC, 0 store canonicalization, 0 plan classification, 0 proposal-outcome derivation; those remain exclusively backend-owned. proposal_outcomes (RECUSADA/APROVADA, by store/department) now drives 'Propostas Recusadas/Aprovadas Válidas por Loja' directly from the RPC-shaped contract, replacing the old local CPF-dedup/latest-status-wins algorithm entirely (0 hidden fallback). Long-format contract arrays (status_by_store/status_by_bank/plans_by_store_department) stay canonical; wide/matrix table views are render-time-only projections (gestao.js), never stored state. SPF EXTRA's 'Comissão Líquida 70%' now sums the backend-provided spf_70_value field directly rather than recomputing ×0.70 locally — same number, different (correct) authority; does NOT resolve the separate, still-open spf_liquido_percentual architectural-debt question (docs/GESTAO-COMMISSION-DECISION.md), which remains BUSINESS_RULE_REVIEW and was not touched. gestao-parity-test.py continues to run and pass, but now validates only the retired engine's historical correctness (tests/fixtures/_gestao-legacy-upload-engine-harness.html) — it is no longer a statement about the active render path. Local-only synthetic sample data (tests/fixtures/gestao-rpc-fixtures.json) — no production/customer data. Frontend diff limited to: gestao.js rewritten to consume the RPC-shaped contract with canonical-code selector values; gestao.adapter.js rewritten thin; 2 new files (gestao-fixture-provider.js, store-display.js); UI visual language, card hierarchy, table structure, and no-horizontal-scroll behavior preserved unchanged. No push, no deploy, no Supabase connection, no production access this Wave.",
      "notes": "RC BLOCKER #6 (PORTAL-NEXT-01.1) investigated PORTAL-NEXT-06, not resolved by fiat that Wave: re-scoped from 'two competing formulas' to the precise, narrower finding in docs/COMMISSION-RULE-MAP.md. Resolved by explicit human business decision in PORTAL-NEXT-06.1 (docs/GESTAO-COMMISSION-DECISION.md, STATUS: HUMAN DECIDED) — 70% fixed for this metric only, not generalized. PORTAL-NEXT-06.1 also fixed a real table header/value alignment bug across all 8 Gestão tables (shared right-aligned axis contract). 'Gestão' in production is a MENU CATEGORY (grupo:'📊 Gestão') containing two sibling tiles — 'Análise F&I do Grupo' (this module, analise-fi-grupo.html) and 'Análise Geral do Grupo' (the separate 'dashbi' module, analise-geral-grupo-secure-original-layout.html, a different and much larger file, Wave 5, unaffected by this Wave)."
    },
    {
      "id": "dashbi",
      "permissionId": "dashbi",
      "authMode": "PERMISSION_MATRIX",
      "name": "Análise Geral do Grupo (Dashbi)",
      "route": "/dashbi",
      "title": "Análise Geral do Grupo",
      "density": "HIGH (dense tables + Model Analysis region) — see docs/DASHBI-KPI-CONTRACT.md",
      "authRequirement": "session + role scope, server-enforced (Dashbi Phase 2, Real Data Integration Foundation: real transport wired against operational_metrics/operational_model_metrics, p_group_view server-gated to VENDEDOR/ANALISTA holding the real 'dashbi' permission — DASHBI_GROUP_VIEW_AUTHORITY_V1; fixture transport remains the deterministic-test default per isRealTransport())",
      "designPattern": "data-table (Approved normative pattern, same one every prior module uses) — NO executable Approved Reference exists for Dashbi; NORMATIVE COMPLIANCE reported, not VISUAL PARITY (Gate 69/121)",
      "entryPoint": "assets/js/dashbi.js + assets/css/dashbi.css + assets/js/adapters/dashbi.adapter.js (PORTAL-NEXT-07)",
      "businessSource": "modules/analise-geral-grupo-secure-original-layout.html — processBase01/processBase02/buildB03Index/aggregate/modelRowsUnified/modelExtraMetrics/rankingFromViews/buildNovosLojaRows and 90 more pure functions (96 total) — EXTRACTED, byte-identical, from origin/main (this file, like Score/Coparticipado, IS divergent from the local clone — see docs/DASHBI-FUNCTION-MAP.md, docs/RANKING-FUNCTION-MAP.md, docs/NOVOS-POR-LOJA-FUNCTION-MAP.md)",
      "functionalSource": "origin/main:modules/analise-geral-grupo-secure-original-layout.html (portal-financiamento-brabus-secure) == live production (PROVED, same byte-hash method as prior Waves; re-confirmed unchanged PORTAL-NEXT-07.1, SHA 2f17eb2341c5cc14aa8710aa044103002ca572a9)",
      "approvedReference": null,
      "migrationWave": 5,
      "risk": "HIGH",
      "migrationStatus": "HUMAN_APPROVED",
      "humanApprovalNote": "PORTAL-NEXT-07.5.2 completed at commit cc3a296 (26/26 goldens, business diff 0, Ranking = Vendedores + Lojas with Departamentos removed, Model Analysis unchanged, no-horizontal-scroll PASS). Human explicitly responded \"Aprovado.\" after that report — reconciled here in PORTAL-NEXT-07.6.1 (this session's own record had gone stale, still showing UAT_PENDING through PORTAL-NEXT-07.6; corrected, not re-approved). PORTAL-NEXT-07.6 subsequently proved Dashbi remained byte-identical (dashbi.js/dashbi.css/dashbi.adapter.js hash 0 diff) and 26/26 goldens continued to pass, both re-confirmed again this Wave. FROZEN as of this status: any future change requires a Design System Change Proposal + new human approval (Skill's Human Approved Freeze rule). This migrationStatus field covers BUSINESS approval only — see the separate 'responsiveRemediation' note below for Dashbi's own RESPONSIVE status, which is NOT HUMAN_APPROVED (a real readability defect was found and fixed in PORTAL-NEXT-07.6.2, after this note originally — incorrectly — claimed RESPONSIVE was also already approved).",
      "responsiveRemediation": "PORTAL-NEXT-07.6.2 — human visual UAT rejected the PORTAL-NEXT-07.6 narrow-width table recomposition: not horizontal scroll, but readability (Financiamentos/Share/Produção Total/Receita Total wrapping character-by-character, currency values splitting across lines). Root cause found (not just patched): .dbTableExpandable{table-layout:fixed} stayed active even with primary rows flexed — with a + Detalhes row open at narrow width, the fixed column-width algorithm squeezed every primary cell to a few px; reproduced directly, fixed with table-layout:auto inside the media query (the same class of fix already needed for Coparticipado's <colgroup> in PORTAL-NEXT-07.6, not yet applied here since this exact interaction — detail row open + narrow width — wasn't tested together until now). Also redesigned the stacked layout itself from generic flex-wrap-packing to an explicit deterministic record: every field defaults to full row width, one per line; only Vendas+Financiamentos (both always a short integer) share a row, per explicit human spec. 0 business change (dashbi.js/dashbi.adapter.js byte-identical, 0 diff; 26/26 goldens unchanged); only dashbi.css changed. RESPONSIVE: UAT_PENDING pending a fresh human visual pass — see docs/HUMAN-UAT-RESPONSIVE-REMEDIATION.md. BUSINESS status (HUMAN_APPROVED, reconciled PORTAL-NEXT-07.6.1) unaffected. PORTAL-NEXT-07.6.3 — investigated a second human rejection: found and killed 14 zombie `python -m http.server 8700` processes (7 simultaneously LISTENING), restarted exactly one clean instance; re-verified 07.6.2's code exhaustively (all fixture/view/viewport/detail-state combinations) against the real served route with 0 problems found — 0 code change this Wave. PORTAL-NEXT-07.6.4 — human UAT rejected AGAIN even after the clean-server/hard-refresh retest, ruling out cache as the explanation. Strategy changed: dedicated non-table mobile card renderers (dbMobileCard()/.dbMobileOnly, plain <div> markup) now render alongside the untouched desktop tables (.dbDesktopOnly) for store/seller/Ranking/Novos por Loja (extended to all 4 since they share the same expandableRow()/expandableTableHtml() component and the same underlying defect pattern), both fed by the same computed row data in the same function call — field-for-field parity verified directly, not assumed. Proven structurally different via computed style: at 390px, 0 visible <table> elements, >0 visible .dbMobileCard elements. detailDomId()/detailToggleHtml()/kpiDetailPanelHtml() gained an optional idSuffix parameter so the two renderers' detail panels never collide as duplicate DOM ids (every pre-existing call site, including Model Analysis's, omits it and is byte-identical to before). 0 business change (dashbi.adapter.js byte-identical; 26/26 goldens, Ranking/Model Analysis/primary KPI hierarchy all re-verified unchanged); only dashbi.js/dashbi.css changed. Exhaustive sweep (12 Score fixtures + 3 Dashbi views + Ranking/Novos por Loja, all x 7 viewports, with a detail toggle opened): 0 problems. RESPONSIVE remains UAT_PENDING pending a fresh human visual pass on this structurally different presentation.",
      "landingTitle": "Análise Geral do Grupo",
      "landingDesc": "Visão analítica geral do Grupo Brabus Mitsubishi.",
      "functionalParity": "PASS — 26/26 golden fixtures, exact match (tests/dashbi-parity-test.py; 24 from PORTAL-NEXT-07 + 1 Ranking tie-break fixture from PORTAL-NEXT-07.1 + 1 Model Analysis parcelamento-completeness fixture from PORTAL-NEXT-07.3), goldens generated by executing an independently re-extracted reference copy (tests/fixtures/_dashbi-reference.js, separate script/algorithm/wrapper) in a real browser. Covers plan classification + priority collision, Base03 positional-alias fallback, duplicate-B3 scoring, the historical/2026-06-01 date-cutoff split, Base01/02 'Nova' format adapters with successful AND failed chassi matching (Entrada diagnostic traceable in both cases), Model Analysis (volume/financiada/penetração/produção/receita/receitaSPF/ticket/retorno/prazo médio/parcela média/balão médio/qtd linear/balão/reversão/Entrada/Entrada Média/Entrada %, family-level totals, plan-mix with percentages at family/model/loja level, special-plan detail, Inconsistências TRITON), the missing-seller hard-stop, FECHAMENTO, Ranking (Top 10 Vendedores/Lojas/Departamentos incl. stable tie-break), Novos por Loja, and cross-module plan-priority consistency with Análise F&I/Coparticipado.",
      "normativeCompliance": "PASS on every data-table.md MUST: tables stay tables, right-aligned tabular-nums headers+values sharing one axis (PORTAL-NEXT-06.1 lesson, reapplied to the new Ranking/Novos por Loja tables this Wave), sticky first column + horizontal scroll on mobile, HIGH-density Ambient Motion OFF. KPI hierarchy reflects materiality; Model Analysis (Gate 81) always shows Entrada/Entrada Média/Entrada %, never hidden for visual simplicity. Anti-AI audit PASS: Ranking uses a plain numbered table (no medals/podium/hero cards, unlike production's own decorative layer), vehicle selector uses a compact image+name+accent card (no catalog styling, no shadow/glow/gradient).",
      "visualParity": "NOT APPLICABLE — no executable Approved Reference exists for Dashbi (Gate 69/121)",
      "notMigratedThisWave": "Excel/CSV file parsing — 0 file I/O. lojaEfetivaPorMudancaPortal's live data source (a Supabase REST fetch inside carregarMudancasLojaVendedoresDashboard) — extracted byte-identical but its backing array is never populated (0 backend), deterministically reproducing the function's own no-override fallback. Medal-card/podium presentation, CSV export — presentation-layer, not reused as-is (their underlying data IS extracted — see docs/RANKING-FUNCTION-MAP.md). Previous-period comparison deltas were ALSO listed here through PORTAL-NEXT-07 — corrected by FC-1 (GAP-001), see fc1ComparisonRestoration below and docs/DASHBI-FUNCTION-MAP.md's FC-1 correction note; no longer deferred. See docs/DASHBI-FUNCTION-MAP.md for the full deferred-dependency table. (The live secure-API path via operational_metrics/operational_model_metrics — previously listed here as 0-backend — was wired in Dashbi Phase 2, Real Data Integration Foundation: see realDataIntegration below.)",
      "realDataIntegration": "Dashbi Phase 2 (Real Data Integration Foundation), commit 742b298. Stage A closed two authority questions with direct evidence before implementation: (1) group-view elevation is explicitly, deliberately gated server-side to VENDEDOR/ANALISTA holding the real 'dashbi' module permission (GERENTE holds the same module permission but is intentionally excluded per the RPC's own post-incident comment) — DASHBI_GROUP_VIEW_AUTHORITY_V1, frozen; (2) Ticket Médio is unanimously production_value/financed_count across every real occurrence in the V1 ground-truth source (self-labeled 'Ticket Médio Financiado'), not sales_value/sold_count — TICKET_CONTRACTUAL_DERIVATION_ALLOWED, implemented in dashbi-real-view-model.js, no backend change. dashbi-real-provider.js is a thin transport boundary (operational_metrics + operational_model_metrics, p_group_view always true, server gate decides real elevation). dashbi-real-view-model.js uses two techniques: synthetic per-unit sales/fins reconstruction (V1's own proven distribute() pattern) for store/seller/ranking/Novos-por-Loja/KPI views feeding dashbi.adapter.js's existing aggregate()/rankingFromViews()/buildNovosLojaRows() unchanged; DIRECT construction from operational_model_metrics's own real per-model aggregates for Model Analysis, deliberately NOT replicating V1's own real-production gap where operational_metrics (no model field) is the only RPC ever wired in — DASHBI_V1_MODEL_INTEGRATION_DEFECT_NOT_REPLICATED. Model-name normalization reuses the existing, frozen modeloPadrao()/familyOfModel() (real RPC returns fully raw dealership-system model strings). 31/31 new deterministic tests (tests/dashbi-real-provider-test.py); 26/26 existing golden-fixture parity unaffected. Verified against real RPC row data from the live project (71 seller/store/department + 241 store/department/model rows): 0 crashes, exact count reconciliation, sane per-family distribution. Classification: DASHBI_REAL_DATA_TECHNICALLY_HOMOLOGATED_LOCAL — technically proven, human runtime UAT still pending (see the Dashbi Phase 2 report). This field covers backend-wiring status only — migrationStatus (business/functional approval) and responsiveRemediation (visual/responsive UAT) above are separate, unaffected axes.",
      "crossModuleConsistency": "PASS — plan classification priority (SUBSIDIADO>REVERSÃO>COPARTICIPADO>BALÃO>LINEAR) proved identical across Dashbi, Análise F&I do Grupo, and Coparticipado for equivalent synthetic inputs (production's own source comments in Dashbi and Gestão explicitly cross-reference each other's rule) — see docs/DASHBI-CROSS-MODULE-CONSISTENCY.md. KPI-level equality (Vendas/Financiamentos/Produção/Receita) was NOT required or tested — Dashbi (Base01×02×03 crossing) and Gestão (single-worksheet with row inheritance) read from different real data universes with different real column contracts; equality was never presumed and is not claimed.",
      "uatRestoration": "PORTAL-NEXT-07.1 — human UAT on PORTAL-NEXT-07 found 4 issues, all addressed: (1-2) Classificação dos Planos + Análise por Modelos moved to Novos-only visibility, matching production's own gating (updateModelosTabVisibility, confirmed by direct source read — this was always a production business rule, not merely a human preference); (3) vehicle images restored from the real production VEHICLE_IMAGES asset (origin/main, decoded+resized, see docs/DASHBI-VEHICLE-ASSETS.md) — not reconstructed from memory or sourced externally; (4) Ranking (all 3 views, Top 10 Vendedores/Lojas/Departamentos) and Novos por Loja (Novos-only) restored with their real business logic extracted byte-identical (see docs/RANKING-FUNCTION-MAP.md, docs/NOVOS-POR-LOJA-FUNCTION-MAP.md). Also fixed, self-caught: a pre-existing PORTAL-NEXT-07 mobile bug where the dev-only diagnostic JSON dump (unbroken string) caused page-level horizontal scroll at all 3 required breakpoints — narrow CSS fix (overflow-wrap), 0 business-logic change.",
      "selectiveNavigation": "PORTAL-NEXT-07.2 — human UAT on PORTAL-NEXT-07.1 confirmed the restored content itself was correct but flagged that Análise por Modelos/Ranking/Novos por Loja must not stay expanded simultaneously, matching the previous Portal's one-surface-at-a-time interaction. Audited production's own showTab() (origin/main lines 4944-4961): a single-active-region tab switcher already exists there (exactly one of share/modelos/ranking/novosLoja gets display:block at a time), gated by updateModelosTabVisibility() with the same per-view availability already established in 07.1 (Grupo/Seminovos: Visão Geral + Ranking only; Novos: all 4). V2 now reproduces this with a compact mode switcher (currentMode state, reusing the existing .dbBtn segmented-control visual grammar already used for Visão/Período — no dedicated Design System tabs pattern exists, so this was the correct authority per Gate 2's own instruction not to blindly implement literal tabs). Plan Classification moved to render only inside the Análise por Modelos mode (confirmed by direct source read that production's planoDestaqueFamiliaAtualHtml is literally emitted inside renderModelos()'s own innerHTML, not a separate block). Real, disclosed finding: production's own fallback-on-view-change is asymmetric — updateModelosTabVisibility() explicitly falls back to the Share tab when Análise por Modelos becomes unavailable, but has no equivalent branch for Novos por Loja (apparently an oversight from when that tab was added in a later UX iteration, per its own source comments) — closing that gap, V2 applies the same deterministic Visão-Geral fallback to both, per this Wave's own explicit 'no blank page' requirement. 0 change to Ranking/Novos por Loja/Model Analysis business logic, vehicle assets, or any already-approved metric (dashbi.adapter.js and all fixtures byte-identical to 07.1 — confirmed via git diff, 0 lines). 25/25 golden fixtures unchanged. Still UAT_PENDING — not auto-promoted, awaiting this round's human review.",
      "modelAnalysisCompleteness": "PORTAL-NEXT-07.3 — human UAT flagged Análise por Modelos was missing analytical columns present in current production (named examples: Balão Médio, Parcela Média — explicitly 'not the complete scope'). Rebuilt the full production inventory from a fresh read of renderModelos() (docs/MODEL-ANALYSIS-PRODUCTION-INVENTORY.md): 14 family-level metric boxes (entirely absent from V2), 18 per-model fields (V2 rendered 11, with different labels/ordering), 4 more plan-breakdown tables (V2 had a partial analog of only 1). Root cause: most of the missing data was ALREADY computed by already-extracted functions (modelRowsUnified/familyExtraMetrics/planRowsByStoreForFamily) but never rendered — a presentation gap, not a missing-extraction gap for most items; 3 functions genuinely needed fresh byte-identical extraction (planTotalRowsForFamily, specialPlanDetailRows, inconsistenciaTritonRows), 96 -> 99 functions extracted, 0 mismatches against an independently re-extracted reference. Proved exact semantics before restoring (docs/MODEL-ANALYSIS-METRIC-CONTRACTS.md): 'Parcela Média' = average PMT value in R$ (not installment count); 'Prazo Médio' IS the average-installment-count metric — production has no second, separately-labeled 'Média de Parcelas' metric, contrary to a plausible but unconfirmed assumption; 'Balão Médio' = average balloon value among only balloon contracts, same population as the BALÃO plan-classification flag. Added a new golden fixture (model_analysis_parcelamento_completo) with real non-zero PMT/installment-count/balloon values across 2 real SKUs plus one record with no Base03 match, proving the exclusion-not-zero-inclusion averaging behavior. Deliberate, disclosed presentation choice: restored as one wide table with grouped headers + sticky Modelo column + horizontal scroll, NOT production's own card+modal shape — data-table.md (the actual Design System authority) forbids cardifying tabular data; flagged HUMAN REVIEW REQUIRED on this specific choice. 0 change to Ranking/Novos por Loja/vehicle selector/selective navigation/any already-parity-proven metric (dashbi.adapter.js's existing functions unchanged — git diff shows only additions, 0 deletions to prior business logic). 26/26 golden fixtures (25 + 1 new). Still UAT_PENDING.",
      "noHorizontalScroll": "PORTAL-NEXT-07.4 — human UAT rejected 07.3's wide table (required horizontal scroll at every viewport, including 1920px) and gave a new global directive: user-facing V2 surfaces must not require horizontal scrolling to access material information (docs/DS-CHANGE-PROPOSAL-NO-HORIZONTAL-SCROLL-01.md). Separately confirmed the earlier 'Análise por Modelos button not working' report was a stale-cache incident (hard refresh resolved it, 0 code changes made — no PORTAL-NEXT-07.3.1 hotfix). Every Dashbi table recomposed with a shared 'primary row + inline + Detalhes' component: a compact always-tabular comparison row (Modelo/Volume/Financiamentos/Penetração, plus Produção/Receita Total/Ticket Médio/Retorno Médio at >=768px) with a one-click vertical expansion covering every remaining metric, grouped Financeiro/Retorno/Parcelamento/Entrada/Planos (presentational grouping only, reusing production's own family-miniGrid categories). Multiple model rows may stay expanded simultaneously per explicit human preference (no accessibility blocker found to override it). Same component reused for store/seller tables, all 3 Ranking tables, Novos por Loja, and the 3 plan-mix tables — all of which also required horizontal scroll at mobile/tablet widths. Full production x V2 completeness re-verified against the frozen 07.3 inventory (docs/MODEL-ANALYSIS-PRIMARY-DETAIL-MAP.md): 0 unmapped, 0 inaccessible material items. Global audit (docs/V2-HORIZONTAL-SCROLL-AUDIT.md) found every HUMAN_APPROVED/FROZEN module (Landing/Score/Coparticipado/Gestão) also has at least one horizontal-scroll dependency — none were modified this Wave; reported as FROZEN CONFLICT, pending an explicitly-authorized future remediation Wave. 0 business-logic change (git diff: 0 lines in dashbi.adapter.js/_dashbi-reference.js/dashbi-fixtures.json). 26/26 golden fixtures unchanged. Automated sweep: 48/48 view x mode x viewport combinations pass with 0 horizontal overflow (detail panels open); full 26-fixture x 3-family sweep at 390px with every detail expanded: 0 overflow, 0 console errors. Still UAT_PENDING.",
      "modelDetailCleanup": "PORTAL-NEXT-07.4.1 — human UAT strongly approved the 07.4 primary+detail experience, with two corrections inside the expanded model detail: 'Entrada Qtd' removed (confirmed, again, never a production metric — only the internal denominator entradaMed/entradaPct divide by, still computed, just not displayed); Qtd Subsidiado and Qtd Coparticipado added to the Planos group. Traced before adding: aggregate()'s own compVals (already extracted byte-identical) computes subsidiadoQtd/coparticipadoQtd on the same compModelo aggregation, same population and classifiers as linearQtd/balaoQtd -- production's own modelRowsUnified simply never surfaced those two fields into its row shape. Sourced from planRowsByModel (already extracted, already on-page), matched by Modelo -- no new business-logic extraction. Reconciliation proved with a golden fixture extended with 1 real Subsidiado and 1 real Coparticipado contract: Subsidiado+Reversão+Coparticipado+Balão+Linear sums exactly to Financiamentos for both tested models, 0 double counting. Order follows official classification priority per explicit human preference. 0 business-logic change (dashbi.adapter.js/_dashbi-reference.js: 0 diff). 26/26 golden fixtures. 0 horizontal overflow reintroduced (re-verified at document and component level). Still UAT_PENDING.",
      "primaryKpiHierarchy": "PORTAL-NEXT-07.5 — new human directive: 5 PRIMARY metrics (Vendas, Financiamentos, Share, Produção Total, Receita Total) must be immediately visible wherever they semantically exist, 0 +Detalhes needed; Receita SPF/Retorno Médio may stay secondary. Full surface audit before any change (docs/DASHBI-ANALYTICAL-HIERARCHY-INVENTORY.md) confirmed Produção/Receita Total already existed as computed, unrendered fields on finLoja/finVendDept/ranking rows — a presentation gap, same recurring pattern as every prior Wave. Confirmed no distinct 'department analysis' V2 surface exists (Ranking's own Departamentos dimension covers it, not invented); confirmed Novos por Loja has no production/financing-value field at all — Produção/Receita Total correctly reported NOT APPLICABLE there, not fabricated (only a derived Share, Financiados/Vendidos, was added). Promoted to primary: Overview KPI grid (Share/Receita Total become their own cards instead of hint text), Loja/Vendedor tables, all 3 Ranking dimensions (Produção Total; Receita Total was already primary as the sort key); Retorno Médio moved into a new KPI-level + Detalhes toggle (the grid never had one before). Share emphasis reuses the already-approved penetracaoCellHtml()/threshold pattern (Model Analysis, 07.3) unchanged — 0 new logic, re-confirmed the 0.40 threshold against current production authority rather than assumed. Receita Total emphasis is new token usage: no exact 'financial value' token exists in design-system-2/tokens.css; --color-accent-primary rejected (already load-bearing for interactive state elsewhere on this page); --color-info selected as a provisional, documented reuse (docs/DS-CHANGE-PROPOSAL-RECEITA-TOTAL-EMPHASIS-01.md, contrast ≈7.66:1 computed not assumed) — applied only to the KPI-grid card, table cells stay plain-formatted (not a rainbow). New .dbTableStackable mobile recomposition (store/seller/Ranking/Novos por Loja only, Model Analysis's own tables untouched) replaces column-hiding with a vertical label/value stack at <=480px so all 5 primary metrics stay outside + Detalhes even on mobile — a real bug (metric cells shrinking to near-zero width and wrapping character-by-character) was caught and fixed during this Wave's own Playwright verification before being reported clean. 0 business-logic change (dashbi.adapter.js/_dashbi-reference.js/dashbi-fixtures.json: 0 diff). 26/26 golden fixtures unchanged. Model Analysis spot-checked (fixture model_analysis_parcelamento_completo): unchanged, not reopened. No-horizontal-scroll matrix re-verified clean at all 6 required viewports, document- and component-level, detail rows expanded. Still UAT_PENDING.",
      "modelAnalysisSimplification": "PORTAL-NEXT-07.5.1 — human decision: removed 4 redundant Análise por Modelos sections (Resumo tipos de plano; Quantidade por tipo de plano / Modelo; Quantidade por tipo de plano / Loja; Detalhe Coparticipado/Subsidiado/Reversão) now that the approved per-model + Detalhes Planos group covers the same quantities. Traced reuse before deleting: planRows (A.planRowsByModel()) feeds both the removed 'Quantidade por tipo de plano / Modelo' table AND the approved + Detalhes merge (subsidiadoQtd/coparticipadoQtd) — only the redundant render call was removed, planRows/the merge/planRowsByModel() itself stay untouched. The other 3 sections' adapter functions (planTotalRowsForFamily/planRowsByStoreForFamily/specialPlanDetailRows) had no other caller and remain intact, simply unused by any V2 renderer now — 0 business logic deleted. 3 now-dead V2 rendering helpers (planPctTableHtml/planPctBodyRow/planPctPrimaryHeaders) removed with their call sites. Information-retention audit (docs/MODEL-ANALYSIS-REDUNDANT-SECTIONS-REMOVAL.md) reported honestly: per-model/family plan-type percentages, the per-loja breakdown, and per-record Cliente/Produção/Receita drill-down are genuinely no longer shown anywhere (marked REVIEW) — per explicit human instruction, no replacement UI was added. 0 business-logic change (dashbi.adapter.js/_dashbi-reference.js/dashbi-fixtures.json: 0 diff). 26/26 golden fixtures unchanged. Verified in a real browser: all 4 removed headings 0 occurrences, no empty shells/orphan headings, + Detalhes Planos group still shows all 5 quantities reconciling exactly, multiple-open still works, no horizontal scroll reintroduced at all 6 viewports. Still UAT_PENDING.",
      "rankingDeptRemoval": "PORTAL-NEXT-07.5.2 — human decision: Ranking's 'Departamentos' dimension removed from the rendered UI ('not required in the final analytical experience'); Ranking now contains exactly Vendedores and Lojas. A.rankingFromViews() is shared across all 3 kinds and stays untouched (still called with 'vendedor'/'loja'); only the 'dept' call + its render were removed from rankingHtml(). vendasDept/finDept (the underlying aggregation in aggregate()) were already unused by any other V2 surface before this change and remain fully intact in the adapter — 0 business logic deleted. 0 business-logic change (dashbi.adapter.js/_dashbi-reference.js/dashbi-fixtures.json: 0 diff; dashbi-parity-test.py never called rankingFromViews() to begin with, so 26/26 golden fixtures are unaffected by construction). Verified in a real browser: Ranking shows exactly 2 sections (Vendedores, Lojas) with full primary headers and working + Detalhes, ends naturally after Lojas with no blank gap; Model Analysis (07.5.1 removals stay removed), Overview KPI grid, Novos por Loja unchanged; no horizontal scroll reintroduced at all 6 viewports. Still UAT_PENDING.",
      "fc11PresentationRevision": "FC-1.1, Human UAT revision of GAP-001 (2026-09-05) — Human UAT on FC-1 confirmed the comparison engine worked but rejected its presentation and did not approve HUMAN_APPROVED. Three changes: (1) COMPARISON PRESENTATION — the percent-delta badge ('▲ +12,4%', 'Anterior: X') was replaced with CURRENT VALUE / PREVIOUS VALUE + DIRECTION ARROW ONLY (▲/▼/→/—, no percent, no label); calcDelta (byte-identical (c-p)/p extraction) stays in dashbi.adapter.js, unused by the UI since this revision — the human explicitly allowed keeping it calculable internally. Previous value uses the exact same formatter as current at every call site; assistive tech gets equivalent context via a visually-hidden .srOnly span. (2) GENERAL_SELLER_TABLE -> REMOVED_FROM_V2_GENERAL_VIEW -> REDUNDANT_WITH_RANKING — 'Vendas e Financiamentos por Vendedor' removed from Visão Geral by explicit human product decision (Ranking is the dedicated seller-performance surface); it existed since the original PORTAL-NEXT-07 Dashbi migration and briefly gained comparison support in FC-1 before this removal — not something that 'never existed'. Capability check against Ranking: DETAIL_CAPABILITY_PRESERVED_BY_RANKING (same metric set; disclosed nuance: Ranking shows top 10 by Receita Total, the removed table showed every seller by Vendas qtd). sellerTableHtml()/SELLER_HEADERS deleted from dashbi.js; underlying aggregate data untouched. (3) SUBNAV REPOSITION — Visão Geral/Análise por Modelos/Ranking/Novos por Loja moved from the bottom of #dbPanel (reachable only after scrolling past KPIs/tables) to a new static #dbSubnav shell slot between the header and the filters (pageShellHtml()); one instance only, active-state/filter-persistence/Novos-por-Loja-availability rules unchanged. See docs/DASHBI-COMPARISON-CONTRACT.md's FC-1.1 section. 26/26 golden fixtures + 36/36 real-provider + 16/16 comparison-parity all still green (business logic and dual-period engine untouched by this revision); 27/27 new tests (tests/dashbi-fc11-presentation-test.py). Classification: GAP001_DASHBI_PRESENTATION_AND_NAVIGATION_TECHNICALLY_HOMOLOGATED — technical/mocked verification complete; live MASTER/VENDEDOR homolog UAT still the pending human checkpoint (same evidentiary gap as FC-1's own note below).",
      "fc1ComparisonRestoration": "FC-1 (GAP-001), V1×V2 Feature Completeness Audit (2026-09-04), commit pending — restored previous-comparable-period deltas (▲/▼/▬, day-aligned via getPreviousMonthComparablePeriod/sameDayPreviousMonth, NOT naive calendar-month) across the 4 surfaces production compared: KPI grid (Vendas/Financiamentos/Share/Produção Total/Receita Total/Receita/Receita SPF/Retorno Médio), Vendas e Financiamentos por Loja/por Vendedor tables, and Model Analysis (all 19 non-identity columns). Score and Análise F&I do Grupo confirmed to have NEVER had this capability in V1 either (audit finding) — not touched, correctly out of scope. Corrected a prior 'presentation only' classification (docs/DASHBI-FUNCTION-MAP.md): compute(fixture) had a single-period signature, so restoring comparison required dual-period data orchestration (SAME_PIPELINE_DIFFERENT_PERIOD: compute() called twice, once per period), not just UI wiring — real transport now fetches the previous comparable period through the same operational_metrics/operational_model_metrics RPCs (dashbi-real-provider.js's loadDashbiRealWithComparison), independently of the current period's own success (previous-fetch failure renders current normally with comparison omitted, never destroys the dashboard). Fixture/demo mode has no date range to derive a previous period from (compute() is period-agnostic) — comparison there is an explicit second fixture pick ('Comparar com'), defaulting to none, so every existing golden-fixture path is untouched (26/26 unaffected). 16/16 new comparison-parity checks (tests/dashbi-comparison-parity-test.py: calcDelta edge cases, day-alignment across January/leap-February/30-day-month/custom multi-month ranges, scope preservation) + 2 new real-provider checks (tests/dashbi-real-provider-test.py, now 33/33) — both green. Classification: GAP001_DASHBI_PREVIOUS_PERIOD_TECHNICALLY_HOMOLOGATED_LOCAL — technically proven against golden fixtures and a mocked real-transport harness; live MASTER/VENDEDOR homolog UAT still pending (this session had no live login session to exercise, same evidentiary gap Dashbi Phase 2's own realDataIntegration note above already flags for its own real-transport work). Medal-card/podium presentation, CSV export (exportarResumoCSV, GAP-004), and Score's/Análise F&I's own gaps remain untouched, explicitly out of this Wave's scope (FC-2/FC-3/FC-4).",
      "notes": "'Gestão' in production is a MENU CATEGORY (grupo:'📊 Gestão') containing two sibling tiles — this module ('Análise Geral do Grupo', dashbi) and the already-migrated 'Análise F&I do Grupo' (gestao). Largest module migrated so far (96 functions extracted, vs. Gestão's 38 and Coparticipado's 40). Real architecture, historically risky per the brief's own account, re-verified not presumed: Base01/02 have a hard 2026-06-01 date-cutoff split between 'historical' and 'Nova' (new) format, each with its own adapter; Base03 supports a positional column E/F alias fallback for 'anonymized' exports (getTCDevolvidaValue/getCodigoIFValue); Model Analysis's Entrada/Entrada Média/Entrada % has a real eligibility guard (valorVenda>0 && financiado>0 && financiado<=1.15×valorVenda) and a traceable diagnostic (chassisLocalizados/chassisNaoLocalizados) instead of an opaque zero — all reproduced byte-identical and covered by dedicated golden fixtures. PORTAL-NEXT-07's own production-surface audit was incomplete (Plan Classification/Model Analysis visibility, vehicle images, and Ranking/Novos por Loja were all missed or wrongly scoped) — corrected in PORTAL-NEXT-07.1 via a from-scratch surface inventory (docs/DASHBI-PRODUCTION-SURFACE-INVENTORY.md), not by trusting the prior Wave's notes."
    },
    {
      "id": "simulador-novos",
      "permissionId": "simuladorCompleto",
      "authMode": "PERMISSION_MATRIX",
      "name": "Simulador Novos",
      "route": "/simulador-novos",
      "title": "Simulador F&I Brabus Mitsubishi",
      "density": "MEDIUM",
      "authRequirement": "session required",
      "designPattern": "financial-simulator (Approved Component/Pattern)",
      "entryPoint": "assets/js/simulador-novos.js + assets/css/simuladores.css (PORTAL-NEXT-08.1), consuming assets/js/adapters/simulador-novos.adapter.js + simulador-shared.adapter.js + financiamento-campanha.adapter.js + cash-conversion.adapter.js (PORTAL-NEXT-08, frozen)",
      "businessSource": "modules/simulador-novos.html — baseCalculoLinear/taxaPricePorIteracao/calcTrad/calcPeriod/calcParcelaUnica (NOT ported — see business-logic-scanner.py)",
      "functionalSource": "modules/simulador-novos.html (portal-financiamento-brabus-secure)",
      "approvedReference": "financial-simulator",
      "migrationWave": 6,
      "risk": "HIGHEST",
      "migrationStatus": "HUMAN_APPROVED",
      "landingTitle": "Simulador de Novos",
      "landingDesc": "Simulação de financiamento para veículos novos.",
      "notes": "RC BLOCKER open (PORTAL-NEXT-01.1 Blocker #5): requires a dedicated pure-function extraction sub-project BEFORE any UI work. Highest financial-materiality surface in the Portal. Duplicated logic with simulador-seminovos, not shared.",
      "engineExtractionNote": "PORTAL-NEXT-08 — discovery + engine extraction + parity harness (no UI migration, no formula changes). Real production source (saved locally from git show origin/main, 0 live connection) revealed 9 independent calculation engines, not one pipeline: Tradicional/Balão, Semestral/Anual, Parcela Única, Financiamento Linear, Descobridor de Taxa, Taxas Subsidiadas, Antecipação, a Semestral Triton/Outlander campaign, and a shared Financiamento Campanha/Coparticipado iframe engine (confirmed byte-identical to Seminovos' own copy) — plus the shared Cash Conversion tool. Each extracted as a pure (no DOM) re-derivation in assets/js/adapters/simulador-novos.adapter.js (+ simulador-shared.adapter.js for provably shared helpers/constants, financiamento-campanha.adapter.js and cash-conversion.adapter.js for the shared engines), verified via tests/simulador-novos-parity-test.py (40/40) by driving the real, unmodified, DOM-coupled production functions in a real browser against the saved origin/main HTML (0 network — loading it as a top-level page structurally forces the FALLBACK-only code path). See docs/SIMULATOR-ENGINE-DISCOVERY-08.md. ENGINE STATUS: PARITY_VERIFIED / FROZEN — this technical designation is distinct from and unaffected by the module's own migrationStatus below (Gate 3, PORTAL-NEXT-08.5).",
      "uiMigrationNote": "PORTAL-NEXT-08.1 — native V2 functional UI built on Red Precision (design-system-2.1/NORMATIVE.md) + the APPROVED financial-simulator reference (facelift-prototype-01/index.html#simulador — .field/.segmented/.inputAffix/.resultHero/.resultSecondaryGrid grammar reused verbatim). Exposes all 9 reachable/authorized Novos engines as a native <select> mode switcher (Gate 8: avoids a 9-item horizontal tab strip). 0 business logic in the page — every number comes from the frozen PORTAL-NEXT-08 adapter. Cash Conversion integrated as its own mode via the frozen shared adapter. Known UI limitation (disclosed, not hidden): Antecipação's balloon-payment sub-feature is not yet exposed in this UI (the extracted engine supports it; the form does not this Wave). SUPERSEDED for navigation/presentation specifics by PORTAL-NEXT-08.2/08.3/08.4 (see humanApprovalNote) — the native <select> mode switcher described here was replaced by grouped mode-nav buttons in 08.2.",
      "humanApprovalNote": "PORTAL-NEXT-08.5, Gate 1/2: human explicit approval (\"Fechamos os simuladores.\") — closes the Simulador Novos UAT cycle begun at PORTAL-NEXT-08.1 and refined through 08.2/08.3/08.4. Business/Functional: HUMAN_APPROVED/FROZEN. UI: HUMAN_APPROVED/FROZEN. Responsive: HUMAN_APPROVED/FROZEN (8-viewport sweep, 0 horizontal overflow, re-verified 08.4). This migrationStatus field covers exactly these 3 dimensions; the underlying extracted ENGINE keeps its own separate, unrewritten PARITY_VERIFIED/FROZEN designation (see engineExtractionNote) — human approval of the migrated UI/business experience is not the same claim as re-verifying the engine's mathematical parity, per Gate 3. FROZEN means: no future Wave may change Simulador Novos business logic, UI composition, responsive behavior, labels, navigation, or financial presentation as a side effect of unrelated work — any further functional/visual change requires explicit new human authorization (Design System Change Proposal + approval, same Freeze rule already applied to Landing/Coparticipado/Gestão/Dashbi). The one named future exception: a later, explicitly-authorized Visual Polish/Motion transversal Wave may touch presentation only, and must preserve business rules, financial outputs, and the approved information hierarchy. Full approved contract and closure record: docs/SIMULATOR-HUMAN-UAT-CLOSURE-08-5.md."
    },
    {
      "id": "simulador-seminovos",
      "permissionId": "simuladorSeminovos",
      "authMode": "PERMISSION_MATRIX",
      "name": "Simulador Seminovos",
      "route": "/simulador-seminovos",
      "title": "Simulador de Financiamentos Seminovos Grupo Brabus",
      "density": "MEDIUM",
      "authRequirement": "session required",
      "designPattern": "financial-simulator (Approved Component/Pattern)",
      "entryPoint": "assets/js/simulador-seminovos.js + assets/css/simuladores.css (PORTAL-NEXT-08.1), consuming assets/js/adapters/simulador-seminovos.adapter.js + simulador-shared.adapter.js + financiamento-campanha.adapter.js + cash-conversion.adapter.js (PORTAL-NEXT-08, frozen)",
      "businessSource": "modules/simulador-seminovos.html (NOT ported — same pattern as simulador-novos, independently duplicated in V1 itself)",
      "functionalSource": "modules/simulador-seminovos.html (portal-financiamento-brabus-secure)",
      "approvedReference": "financial-simulator",
      "migrationWave": 6,
      "risk": "HIGHEST",
      "migrationStatus": "HUMAN_APPROVED",
      "landingTitle": "Simulador de Seminovos",
      "landingDesc": "Simulação de financiamento para veículos seminovos.",
      "notes": "Same RC BLOCKER as simulador-novos (#5) — the extraction sub-project should very likely produce ONE shared engine for both, ending V1's own duplication, but that is a Wave 6 design decision, not decided here.",
      "engineExtractionNote": "PORTAL-NEXT-08 — same discovery + extraction + parity approach as Novos, executed independently (Gate 30: no assumption that Novos rules apply). Real, reachable engines: Tradicional/Balão (adds a vehicle year-band dimension Novos has no concept of at all), Semestral/Anual, a nested \"Financiamento Seminovos\" RATE_TABLE engine (year x entry-band lookup, its own PMT/IOF formula, genuinely different from Novos' Linear), Descobridor de Taxa, Antecipação, a Semestral Triton campaign (shares Novos' core constant but a DIFFERENT final valorFinalVenda formula — bem minus the full rebate, not just Brabus' share), plus the shared Financiamento Campanha/Coparticipado iframe and Cash Conversion. Gate 3/33 finding, verified by direct source inspection: the top-level \"Financiamento Linear\" tab and the entire \"Taxas Subsidiadas\" feature are DEAD CODE in Seminovos — their target DOM elements (#lValorBem/#lEntrada/#lResultados, id=\"subsidizedScreen\") do not exist anywhere in production markup, only in defensively-guarded JS that safely no-ops; kept in the adapter for function-inventory completeness only, excluded from the parity requirement (no reachable production path to verify against). tests/simulador-seminovos-parity-test.py: 24/24. tests/simulador-cross-product-test.py: 5/5 (confirms Novos/Seminovos correctly diverge on independent engines and correctly match on genuinely shared ones). PORTAL-NEXT-08.4 EXTENDED this finding: Semestral/Anual, Financiamento Campanha/Coparticipado, and Semestral Triton were also proved unreachable in production by the identical evidentiary standard (0 matching entry-button/tab markup) — see uiMigrationNote below. See docs/SIMULATOR-ENGINE-DISCOVERY-08.md. ENGINE STATUS: PARITY_VERIFIED / FROZEN — this technical designation is distinct from and unaffected by the module's own migrationStatus below (Gate 3, PORTAL-NEXT-08.5).",
      "uiMigrationNote": "PORTAL-NEXT-08.1 — same approach as Novos, executed independently. Exposed the 6 reachable Seminovos engines identified at that time — Gate 47 dead-code guard: the confirmed-dead top-level \"Financiamento Linear\" and \"Taxas Subsidiadas\" features (0 matching production DOM markup, see PORTAL-NEXT-08 discovery) are deliberately NOT exposed as modes, even though the adapter still carries their pure re-derivations for function-inventory completeness. \"Financiamento Seminovos\" (RATE_TABLE, year x entry-band, includes 50x) is exposed as its own distinct mode, not merged with Novos' Linear. Same known Antecipação balloon-UI limitation as Novos. SUPERSEDED by PORTAL-NEXT-08.4: re-auditing navigation against real production surfaced that 3 of those 6 originally-exposed modes — Semestral/Anual, Financiamento Campanha/Coparticipado, Semestral Triton — are themselves unreachable (0 real click path: entry button/tab markup absent, same standard as the Gate 47 dead features), and were removed from the V2 UI per explicit human decision (\"match production exactly\"). Approved final navigation: FINANCIAMENTO = Tradicional (Balão) + Linear only (label \"Linear\", renamed from \"Financiamento Seminovos\" to match production's own menu card, mode id unchanged); FERRAMENTAS = Descobridor de Taxa, Antecipação de Parcelas, Cash Conversion.",
      "humanApprovalNote": "PORTAL-NEXT-08.5, Gate 1/2: human explicit approval (\"Fechamos os simuladores.\") — closes the Simulador Seminovos UAT cycle begun at PORTAL-NEXT-08.1 and finalized at PORTAL-NEXT-08.4. Business/Functional: HUMAN_APPROVED/FROZEN. UI: HUMAN_APPROVED/FROZEN. Responsive: HUMAN_APPROVED/FROZEN (8-viewport x 5-mode sweep, 0 horizontal overflow, PORTAL-NEXT-08.4). This migrationStatus field covers exactly these 3 dimensions; the underlying extracted ENGINE (simulador-seminovos.adapter.js, byte-identical/untouched through 08.4) keeps its own separate PARITY_VERIFIED/FROZEN designation (see engineExtractionNote), per Gate 3. UI consistency with Novos (shared presentation grammar: mode nav, term grid, balloon story) does NOT imply engine identity — Seminovos retains its own vehicle-year dimension, RATE_TABLE behavior (incl. authoritative 50x), and distinct Tradicional/Balão semantics, documented in PORTAL-NEXT-08's discovery and re-affirmed here (Gate 8, PORTAL-NEXT-08.5). FROZEN means the same as for Novos (see that entry's humanApprovalNote) — no future Wave may change Seminovos business logic, UI, responsive behavior, labels, navigation, or financial presentation without explicit new human authorization; the same named future Visual Polish/Motion exception applies. Full approved contract and closure record: docs/SIMULATOR-HUMAN-UAT-CLOSURE-08-5.md."
    },
    {
      "id": "salarios-comissoes",
      "permissionId": "comissoes",
      "authMode": "PERMISSION_MATRIX",
      "name": "Salários / Comissões",
      "route": "/salarios",
      "title": "Salários e Comissões",
      "density": "MEDIUM",
      "authRequirement": "session + role scope",
      "designPattern": "no dedicated Approved Reference — cross-cutting domain, not a single V1 page",
      "entryPoint": "NOT YET CREATED in V2",
      "businessSource": "assets/js/portal-app.js — commissionCalc()/commissionSummaryFromBlocks() (pure functions, NOT ported this phase)",
      "functionalSource": "assets/js/portal-app.js:commissionCalc (portal-financiamento-brabus-secure)",
      "approvedReference": null,
      "migrationWave": 4,
      "risk": "MEDIUM",
      "migrationStatus": "NOT_MIGRATED",
      "landingTitle": "Salários & Comissões",
      "landingDesc": "Composição de comissão, produção e status da equipe.",
      "notes": "commissionCalc() is already a clean pure function in V1 — good extraction candidate. Same RC BLOCKER #6 as 'gestao': cannot claim reconciled parity with analise-fi-grupo.html's separate hardcoded rule until a human decides."
    },
    {
      "id": "brabus-intelligence",
      "permissionId": null,
      "authMode": "SEPARATE_AUTHORITY",
      "name": "Brabus F&I Intelligence (AI + future Voice)",
      "route": "/intelligence",
      "title": "Brabus Intelligence",
      "density": "MEDIUM (conversation workspace)",
      "authRequirement": "session + MASTER role (server-enforced) — IMPLEMENTED as of IA-V2-2 (real_text mode): real Supabase Auth session via auth-boundary.js; MASTER resolved server-side by the Edge Function via a service-role lookup, never a client claim. Human-approved (IA-V2-2-HUMAN-UAT-02) against a local mock Supabase; real Supabase project homologation pending (IA-V2-3). A server-authoritative kill switch (IA-V2-3B) additionally gates every request after MASTER validation, before any OpenAI call.",
      "designPattern": "V2-native fixture-driven conversation workspace on module-system.css primitives (IA-V2-1) — the prior voice-focus-mode/voice-orb-variant reference is NOT used; this Wave deliberately did not transplant that legacy UI (see docs/IA-V2-1-CONTRACT.md)",
      "entryPoint": "assets/js/brabus-intelligence.js (NX_BRABUS_INTELLIGENCE_PAGE) + assets/js/adapters/brabus-intelligence.adapter.js (NX_BRABUS_INTELLIGENCE_ADAPTER)",
      "businessSource": "N/A this Wave — 0 business logic in V2; every fixture value is a plain synthetic literal, not derived from a formula (see tests/business-logic-scanner.py)",
      "functionalSource": "Contract recovered by reading the real, reconciled portal-ai-homolog Edge Function + assets/js/portal-ai-ui.js (portal-financiamento-brabus-secure, ia-reconciliation-v2-local worktree) — behaviorally, never transplanted",
      "approvedReference": "N/A — see designPattern",
      "migrationWave": 7,
      "risk": "HIGH",
      "migrationStatus": "IN_PROGRESS",
      "landingTitle": "Brabus Intelligence",
      "landingDesc": "Assistente de IA — texto e, futuramente, voz.",
      "notes": "real_text transport implemented and human-homologated locally: Scenario 1 plus 3 selected checkpoints (Balão, Multi-turn, Cash Conversion rate sovereignty), all HUMAN_PASS (IA-V2-2-HUMAN-UAT-02). Structured-block presentation contract corrected to match the real backend's field shapes (IA-V2-2-STRUCTURED-BLOCK-FIX-01). A server-authoritative kill switch was added (IA-V2-3B, migration 20260902100000_ia_texto_kill_switch.sql, key ia_texto_habilitada in public.configuracoes via operational_portal_config()) -- default disabled, fail-closed. Remaining blockers before real Supabase homologation: production CORS/origin decision, Edge Function deployment + secret provisioning, dedicated homolog MASTER account (IA-V2-3A/C). Voice Orb variant human decision and the CSP media-src prerequisite remain open, but only block the later Voice Wave, not this one."
    },
    {
      "id": "painel-analista-fi",
      "permissionId": "painelAnalistaFi",
      "authMode": "ANALISTA_OR_MASTER",
      "name": "Painel do Analista F&I",
      "route": "/painel-analista-fi",
      "title": "Painel do Analista F&I",
      "density": "LOW",
      "authRequirement": "session + perfil in (ANALISTA, MASTER), server-enforced (V1: portal-app.js:2003-2004 hard client gate + RPC-level SECURITY DEFINER re-check)",
      "designPattern": "no Approved Reference yet -- small self-contained surface (~45 lines of V1 controller/render code)",
      "entryPoint": "NOT YET CREATED in V2",
      "businessSource": "N/A (self-service queue-status control, not financial business logic)",
      "functionalSource": "assets/js/portal-app.js:2002-2043 (showPainelAnalistaFi/atualizarResumoPainelAnalistaFi/alterarMeuStatusAnalistaFi) (portal-financiamento-brabus-secure) -- reads RPC operational_my_analyst_fi, writes RPC atualizar_meu_status_analista_fi (both confirmed live/ATIVA, Git=C -- DB-only, not in this repo's migrations)",
      "approvedReference": null,
      "migrationWave": 0,
      "risk": "LOW",
      "migrationStatus": "NOT_MIGRATED",
      "landingTitle": "Painel do Analista F&I",
      "landingDesc": "Controle de status na fila de atendimento: online, ocupado, almoço, férias ou offline.",
      "notes": "AUTH FOUNDATION Phase 2A discovery (Gate 5): a real, live, currently-used surface entirely missed by the original PORTAL-V2 MASTER GAP AUDIT because that audit was built from this registry rather than cross-checked against V1's live modulos_portal permission catalog. No department/store scoping -- role-only gate. Zero overlap with Gestão/Dashbi/Score/Salários (distinct data domain: call-queue availability, table inferred as analistas_fi). Management counterpart is central-atendimento-fi (same underlying queue/table) -- kept as two separate registry entries per AUTH FOUNDATION Phase 2B Decision H3 (different role gates: ANALISTA+MASTER vs MASTER-only), though they may share future V2 components/design primitives."
    },
    {
      "id": "central-atendimento-fi",
      "permissionId": "centralAtendimentoFi",
      "authMode": "MASTER_ONLY",
      "name": "Central de Atendimento F&I",
      "route": "/central-atendimento-fi",
      "title": "Central de Atendimento F&I",
      "density": "MEDIUM-HIGH (dashboard + CRUD form + filterable/exportable history table)",
      "authRequirement": "session + MASTER role, server-enforced (V1: hard isMaster() gate at every render function + 5 SECURITY DEFINER RPCs re-checking server-side)",
      "designPattern": "no Approved Reference yet -- a full mini-SPA admin console (~130 dense lines of V1 controller/render code across dashboard/analistas/histórico screens)",
      "entryPoint": "NOT YET CREATED in V2",
      "businessSource": "N/A (admin/queue-management operations, not financial business logic)",
      "functionalSource": "index.html:1639-1761 (portal-financiamento-brabus-secure) -- reads RPCs gestor_listar_analistas_fi/gestor_listar_historico_atendimentos_fi, writes gestor_salvar_analista_fi/gestor_alterar_status_analista_fi/gestor_encerrar_expediente_fi + a queue-reorder pair (all confirmed live/ATIVA, Git=C -- DB-only). Includes a client-side Excel export (XLSX.writeFile) of the call history.",
      "approvedReference": null,
      "migrationWave": 0,
      "risk": "MEDIUM",
      "migrationStatus": "NOT_MIGRATED",
      "landingTitle": "Central de Atendimento F&I",
      "landingDesc": "Administração da fila de atendimento: analistas, status e histórico de chamadas F&I.",
      "notes": "AUTH FOUNDATION Phase 2A discovery (Gate 6) -- same missed-surface finding as painel-analista-fi. The largest and most mature of the two; a real MASTER-only CRUD admin console with genuine write operations (analyst upsert, forced status changes, bulk end-of-shift), not a read-only report. No overlap with Gestão/Dashbi/Score/Salários; it is the management counterpart to painel-analista-fi (same analistas_fi queue/table). Kept as a separate registry entry per Decision H3 (see painel-analista-fi's own notes)."
    }
  ]
}
